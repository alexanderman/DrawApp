angular.module('App')
.config(['config', 'canvasServiceProvider', 'userServiceProvider', 'resourcesListProvider',
    function(config, canvasServiceProvider, userServiceProvider, resourcesListProvider) {

    /**
     * possible place to configure providers
     */

}]);